<Window x:Class="AudioAlign.DtwPathViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:blubb="clr-namespace:AudioAlign"
        Title="DtwPathViewer" Height="750" Width="750"
        Background="{StaticResource WindowBackgroundBrush}" WindowStyle="ToolWindow">
    <Grid>
        <blubb:DtwPath x:Name="dtwPath" HorizontalAlignment="Center" VerticalAlignment="Center" Width="700" Height="700" />
    </Grid>
</Window>
